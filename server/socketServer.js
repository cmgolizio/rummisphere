// Legacy entry point kept for backwards compatibility with earlier scripts.
// The authoritative Socket.IO server implementation now lives in
// `gameServer.js`; importing it here simply boots the same server when this
// file is executed directly.

import "../gameServer.js";
